import 'package:flutter/material.dart';

class DishName extends StatelessWidget {
  const DishName({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}
